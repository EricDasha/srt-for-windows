{
    "name": "srt-for-windows",
    "version": "1.1.0",
    "description": "AnySub Extension for Windows - Now with PiP Support",
    "scripts": {
        "build": "powershell -Command \"if (!(Test-Path dist)) { New-Item -ItemType Directory -Force -Path dist }; Compress-Archive -Path * -DestinationPath dist/srt-for-windows.zip -Force\"",
        "clean": "powershell -Command \"Remove-Item -Path dist -Recurse -Force -ErrorAction SilentlyContinue\""
    },
    "author": "EricDasha",
    "license": "MIT"
}